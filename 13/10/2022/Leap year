#include<stdio.h>
int main()
{
    int a,b,c;
    printf("enter the year:",a);
    scanf("%d",&a);
    printf("enter the date:",b);
    scanf("%d",&b);
    printf("enter the month:",c);
    scanf("%d",&c);
     if(a%4==0)
    {
        printf("given aniversery year is a leap year %d/%d/%d",b,c,a);
    }
    else{
        printf("given aniversary year is a not a leap year %d/%d/%d",b,c,a);
    }
    return 0;
}
